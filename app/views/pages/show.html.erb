<div class="row">
	<div class="span4 columns">
		list of projects goes here.
	</div>
	<div class="span12 columns">
		
		<h1><%= @page.title %></h1>
		
		<% if @page.content_markup == 'markdown' or @page.content_markup.nil? %>
			<%= markdown @page.content %>
		<% elsif @page.content_markup == 'html' %>
			<%= raw @page.content %>
		<% end %>
	</div>
</div>